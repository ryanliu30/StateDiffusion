from .sac_config import sac_config
from .dynamics_config import dynamics_config
from .diffusion_config import diffusion_config

configs = {
    "sac": sac_config,
    "dynamics": dynamics_config,
    "diff": diffusion_config,
}
